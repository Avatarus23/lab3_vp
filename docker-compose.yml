version: "3"
services:
  labdb:
    image: "postgres"
    container_name: "labgrb"
    environment:
      - POSTGRES_USER=bobi
      - POSTGRES_PASSWORD=bobi
      - POSTGRES_DB=music

    ports:
      - "5432:5432"